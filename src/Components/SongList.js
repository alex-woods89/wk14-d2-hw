import React from 'react'
import Song from './Song'

const SongList =({songData}) => {
   const 
    return ( 
        
        <Song title ={songData} />
    )
    
  
    
    }


export default SongList